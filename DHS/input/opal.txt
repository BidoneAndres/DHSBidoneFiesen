
int calcular_suma(int a, int b, int c);
int calcular_resta(int a, int b);


int calcular_resta(int a, int b){
    
    return a-b;
}

void ponerEn0(int x){
    x = 0;
}

int calcular_suma(int a , int b , int c) {
    int x, y, z;
    x = 10;
    y = 20;
    z = x + y + 5 + a + b - c;
    return z;
}

int main() {


    int a, b, c, resultado;

    b = 2;
    c = 4;
    a = b + c * 4;
    

    a = b + 1;
    a = b * 7;




    b = calcular_resta(10, 9);

    while (c == 0) 
        a = a + 2;
  


    while (a <= 10 && b==0 || c == 0) {
        a = a + 2;
    }


    for (b = 0; b < 5 || a + 4*(b+c*a) == 0 && c >= 50; b++) {
        c = c + 1;
    }


    for (b = 0;c >= 50; b++) 
        c = c + 1;
    

    if (a > 50) {
        resultado = 1;
    } else {
        if (c == 9) {
            calcular_suma(5, a + b * c, 1 + 2 );
        } else {
            resultado = 0;
        }
    }


    if (a == 1)
        resultado = 3;

    resultado = calcular_suma();
    
    return resultado;
}